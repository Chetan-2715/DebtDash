<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme — Pure black, no action bar (Compose handles everything) -->
    <style name="Theme.DebtDash" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/background_pure</item>
        <item name="android:statusBarColor">@color/background_pure</item>
        <item name="android:navigationBarColor">@color/background_pure</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>
</resources>
